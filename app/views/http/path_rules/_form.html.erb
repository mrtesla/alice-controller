<%= form_for(@http_path_rule) do |f| %>
  <% if @http_path_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@http_path_rule.errors.count, "error") %> prohibited this http_path_rule from being saved:</h2>

      <ul>
      <% @http_path_rule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :core_application_id %><br />
    <%= f.number_field :core_application_id %>
  </div>
  <div class="field">
    <%= f.label :path %><br />
    <%= f.text_field :path %>
  </div>
  <div class="field">
    <%= f.label :actions %><br />
    <%= f.text_area :actions %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
