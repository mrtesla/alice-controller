<div class="page-header">
  <h1>Routers <small>deal the cards</small></h1>
</div>
<div class="row">
  <%= render 'list', http_routers: @http_routers %>
  <div class="span10"><%= paginate @http_routers %></div>
</div>
